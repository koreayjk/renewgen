{
  "name": "renewgen-lint",
  "version": "1.0.0",
  "private": true,
  "description": "Dev-only syntax linter for the build-less Renewgen site (JSX + PHP). Not deployed.",
  "type": "module",
  "scripts": {
    "lint": "node lint.mjs"
  },
  "dependencies": {
    "@babel/parser": "^7.25.0"
  }
}
